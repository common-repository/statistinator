jQuery( document ).ready( function( $ ) {

    //Auth clear
    /*
    $( ".sts-clear" ).click( function(e) {
        e.preventDefault();
        var name = $( this ).data('name');
        $( "input[name='"+name+"']" ).val('');
        $( this ).after( "<br/><span>Save to store changes</span>" );
        $( this ).closest( 'td' ).find( '.auth-status' ).html( 'Deleted' );
        $( this ).remove();
    });
    */

});

